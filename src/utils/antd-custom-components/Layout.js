import { Layout } from "antd";
import styled from "styled-components";

export const LayoutMainPageStyle = styled(Layout)`
  .ant-layout-content {
    background-color: rgba(255, 255, 255, 0.3);
    padding: 2rem 3rem;
  }
  .layout-content {
    min-height: 300px;
    padding: 1rem;
    background-color: #fff;
  }
`;
